server:
    port: 4000
    error:
        include-message: always

spring:
    application:
        name: template-micro-service
    datasource:
        url: jdbc:postgresql://localhost:5432/gestion_ressources
        username: postgres
        password: 12345678
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql : true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true

yowyob:
    openapi:
        prod-url: http://localhost:4000
        dev-url: http://localhost:4000